# Claude Code

This repository was designed and scaffolded with **Claude Code** assistance.

## How Claude Code was used

- Architecture: NestJS code-first GraphQL + Prisma + Docker Compose
- Domain models from resume 
- GraphQL `MeResolver` queries
- Describe README.md file

## Local Claude Code tips

```bash
# Open this folder in Claude Code and ask for:
# - new GraphQL fields
# - Prisma schema / db push
# - content updates via SQL / DBeaver
```
